name: catching_josh_example
description: "Example app demonstrating how to use the catching_josh package."
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: ^3.6.0
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.5.0
  dio: ^5.9.0
  shared_preferences: ^2.5.3
  catching_josh:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true